<%= form_for @event, html: {class: 'form'} do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label :project_id %><br>
      <%= f.select :project_id, options_from_collection_for_select(@projects, :id, :name, selected: @event.project), {}, {class:'select2'} %>
    </div>
    <div class="form-group">
    <%= f.label :start %><br>
    <%= f.datetime_select :start, {}, html: {class: 'form-control'} %>
    </div>
    <div class="form-group">
    <%= f.label :end %><br>
    <%= f.datetime_select :end, class: 'form-control' %>
    </div>
    <div class="form-group">
    <%= f.submit 'Done', class: 'btn btn-default' %>
  </div>
<% end %>
